/*6. Este script el usuario deberá teclear una letra y un número, y el programa mostrará una cadena formada por la letra repetida el número que haya tecleado.*/
let num = 5, num2 = 0, mensaje = "";     //parseInt(prompt(...))

for (let iterator = 0; iterator < num; iterator = iterator + 1){
    mensaje = mensaje + "x";
    //console.log(mensaje)
}
console.log(mensaje)

/**
 * let texto = prompt("Introduce un caracter");
 * let char = texto[0];     Primer caracter
 */